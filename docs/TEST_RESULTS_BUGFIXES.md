# BUGFIXES End-to-End Test Results

| Test Case | Description (Bug & Root Cause) | Fix | Result | Key Output (Before / After) |
|-----------|--------------------------------|-----|--------|-----------------------------|
| Recovery of backdated session | **Bug:** Resumed session showed incorrect time.<br>**Root Cause:** Client miscalculated elapsed time vs server `start_time` when recovering an active session. | Corrected recovery calculation to accurately reflect elapsed time based on the server's `start_time`. | ✅ Pass | **Before:** `Bug detected: Expected ~12 minutes remaining, but displayed time is 24:59`<br>**After:** `Test passed! The displayed time accurately reflects the server start_time.` |
| Race condition in completeSession | **Bug:** Delayed completion applied to a *new* session.<br>**Root Cause:** `completeSession` read the global `currentSessionId` *after* an async delay (notification sound). | Captured the correct `session_id` locally before the async delay so the PATCH request targets the original session. | ✅ Pass | **Before:** `Bug detected: The PATCH request was sent to the WRONG session ID (999)`<br>**After:** `Test passed! The PATCH request was sent to the original session ID.` |
| Resync of legitimately ended session | **Bug:** Device A got stuck in 'paused' state if Device B ended the timer.<br>**Root Cause:** `loadState()` didn't correctly reset the UI to 'idle' when fetching a session that was already stopped early or completed. | `loadState()` now properly resets the UI to 'idle' if the fetched active session is actually ended. | ✅ Pass | **Before:** `Bug detected: Device A reverted to 'paused' instead of 'idle'. 'Resume' button is a dead-end.`<br>**After:** `Test passed! Device A successfully reset to idle.` |
